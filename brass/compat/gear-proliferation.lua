local rm = require("recipe-modify")
local tf = require("techfuncs")
local parts = require("variable-parts")
local cu = require("category-utils")

--gears are the primary consumer of brass. in vanilla gears are just the "this is mechanical" intermediate.
--many mechanical things nonetheless don't need gears, or just use plates. add them to those recipes here.

--What is the intention with this? -Ashier
